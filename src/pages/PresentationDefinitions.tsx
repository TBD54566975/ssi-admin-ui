import type { Component } from 'solid-js';

const pageTitle = 'Presentation Definitions';

const PresentationDefinitions: Component = (props) => {
    return (
        <article>
            <h1>{pageTitle}</h1>
        </article>
    )
}

export default PresentationDefinitions;